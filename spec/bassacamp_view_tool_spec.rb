require "spec_helper"

describe BassacampViewTool do
  it "has a version number" do
    expect(BassacampViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
